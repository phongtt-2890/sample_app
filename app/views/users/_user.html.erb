<li>
  <%= gravatar_for user, size: Settings.avatar_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= button_to t("delete?"), user, data: { confirm: t("are_you_sure?") }, method: :delete %>
  <% end %>
</li>
